export const NUMBER_OF_USERS = 15000;
export const BATCH_SIZE = 100;
export const RATE_LIMIT_DELAY = 10;
export const AIRDROP_IDS = Array.from({ length: 41 }, (_, i) => BigInt(i)); // Generates an array from 0n to 40n
